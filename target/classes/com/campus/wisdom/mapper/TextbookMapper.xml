<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.wisdom.mapper.TextbookMapper">
    <!-- Textbook getTextbookById(@Param("id") String id) -->
    <select id="getTextbookById" resultType="Textbook">
        SELECT * FROM textbook WHERE id = #{id}
    </select>
</mapper>