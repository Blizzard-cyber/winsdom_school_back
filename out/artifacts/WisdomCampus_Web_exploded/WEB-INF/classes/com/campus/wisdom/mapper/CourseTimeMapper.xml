<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.wisdom.mapper.CourseTimeMapper">
    <!-- CourseTime getCourseTimeById(@Param("id") String id) -->
    <select id="getCourseTimeById" resultType="CourseTime">
        SELECT * FROM course_time WHERE id = #{id}
    </select>
</mapper>